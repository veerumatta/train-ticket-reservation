name: CI/CD Pipeline for train-ticket-reservation Deployment

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build-and-deploy:
    name: Build and Deploy Sequentially
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set Up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Build WAR with Maven
        run: mvn package -DskipTests

      - name: Verify WAR File
        run: ls -lh target/*.war

      - name: Set WAR_FILE variable
        run: echo "WAR_FILE=$(ls target/*.war)" >> $GITHUB_ENV

      # =====================
      # Deploy to DEV
      # =====================
      - name: Deploy WAR to DEV
        run: |
          echo "Deploying $WAR_FILE to DEV"
          curl -v -u "${{ secrets.TOMCAT_USER_DEV }}:${{ secrets.TOMCAT_PASS_DEV }}" \
            -T "$WAR_FILE" \
            "http://${{ secrets.TOMCAT_URL_DEV }}:8080/manager/text/deploy?path=/trainbook-dev&update=true"

      # =====================
      # Deplo





