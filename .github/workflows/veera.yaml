name: CI/CD Pipeline for Java App (Maven + SonarQube + Snyk + Nexus + Tomcat)

on:
  push:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build-deploy:
    runs-on: ubuntu-latest

    env:
      NEXUS_USERNAME: ${{ secrets.NEXUS_USERNAME }}
      NEXUS_PASSWORD: ${{ secrets.NEXUS_PASSWORD }}
      NEXUS_SNAPSHOT_URL: ${{ secrets.NEXUS_SNAPSHOT_URL }}
      NEXUS_RELEASE_URL: ${{ secrets.NEXUS_RELEASE_URL }}

    steps:
      # 1️⃣ Checkout code
      - name: Checkout Repository
        uses: actions/checkout@v4

      # 2️⃣ Set up Java 17
      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      # 3️⃣ Cache Maven dependencies
      - name: Cache Maven packages
        uses: actions/cache@v4
        with:
          path: ~/.m2
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      # 4️⃣ Build WAR
      - name: Build with Maven
        run: mvn clean package -DskipTests

      # 5️⃣ Set WAR file variable
      - name: Set WAR file
        run: |
          WAR_FILE=$(ls target/*.war)
          if [ ! -f "$WAR_FILE" ]; then
            echo "WAR file not found!"
            exit 1
          fi
          echo "WAR_FILE=$WAR_FILE" >> $GITHUB_ENV

      # 6️⃣ SonarQube Analysis
      - name: SonarQube Scan
        run: |
          mvn clean verify sonar:sonar \
            -Dsonar.projectKey=train-ticket-reservation \
            -Dsonar.host.url="${{ secrets.SONAR_HOST_URL }}" \
            -Dsonar.login="${{ secrets.SONAR_TOKEN }}"

      # 7️⃣ Snyk Vulnerability Scan
      - name: Run Snyk to check for vulnerabilities
        uses: snyk/actions/maven@master
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        continue-on-error: true

      # 8️⃣ Upload WAR to Nexus (Snapshot or Release)
      - name: Upload WAR to Nexus
        run: |
          VERSION=$(basename $WAR_FILE | sed -E 's/.*-([0-9]+.*)\.war/\1/')
          echo "VERSION=$VERSION"

          if [[ "$VERSION" == *"SNAPSHOT"* ]]; then
            REPO_URL="${{ secrets.NEXUS_SNAPSHOT_URL }}"
          else
            REPO_URL="${{ secrets.NEXUS_RELEASE_URL }}"
          fi

          echo "Deploying $WAR_FILE version $VERSION to $REPO_URL"

          mvn deploy:deploy-file \
            -DgroupId=TrainBook \
            -DartifactId=TrainBook \
            -Dversion=$VERSION \
            -Dpackaging=war \
            -Dfile=$WAR_FILE \
            -DrepositoryId=nexus \
            -Durl=$REPO_URL \
            -Dusername=$NEXUS_USERNAME \
            -Dpassword=$NEXUS_PASSWORD

      # 9️⃣ Check Tomcat connection
      - name: Check Tomcat Connection
        run: |
          curl --fail --anyauth -u "${{ secrets.TOMCAT_USER }}:${{ secrets.TOMCAT_PASSWORD }}" \
          "http://${{ secrets.TOMCAT_HOST }}:8080/manager/text/list"

      # 🔟 Deploy WAR to Tomcat
      - name: Deploy WAR to Tomcat
        run: |
          curl -v -u "${{ secrets.TOMCAT_USER }}:${{ secrets.TOMCAT_PASSWORD }}" \
          -T "$WAR_FILE" \
          "http://${{ secrets.TOMCAT_HOST }}:8080/manager/text/deploy?path=/REDDY&update=true"














          




          


